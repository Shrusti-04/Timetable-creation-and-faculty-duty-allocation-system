// This file contains environment configuration for the frontend
const config = {
  // Change this to your Glitch backend URL when deployed
  API_URL: import.meta.env.VITE_API_URL || "https://your-glitch-app.glitch.me",
};

export default config;
